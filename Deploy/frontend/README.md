# Frontend UI Service

Streamlit веб-интерфейс для сервиса распознавания диаграмм. Предоставляет простой и интуитивный способ загрузки диаграмм и просмотра результатов.

## Структура

```
frontend/
├── app/
│   ├── main.py              # Streamlit приложение
│   └── requirements.txt     # Python зависимости
├── Dockerfile
├── .dockerignore
└── README.md
```

## Функциональность

### Основные возможности

1. **Загрузка файлов** - поддержка drag-and-drop и выбор файла
2. **Превью изображений** - отображение загруженной диаграммы
3. **Распознавание** - отправка в backend и получение результата
4. **Статистика** - отображение метрик работы системы
5. **История** - просмотр последних запросов

### Интерфейс

**Главная страница (Распознавание):**
- Информация о поддерживаемых форматах
- Загрузка файла
- Превью изображения
- Кнопка распознавания
- Отображение результата в виде markdown таблицы
- Метаданные обработки

**Вкладка История:**
- Список последних 20 запросов
- Детальная информация по каждому запросу
- Кнопка обновления

**Sidebar:**
- Статус backend сервиса
- Поддерживаемые форматы
- Общая статистика
- Статистика по устройствам

## Сборка и запуск

### Сборка образа

```bash
docker build -t frontend-ui:latest .
```

### Запуск контейнера

```bash
docker run -d \
  --name frontend-ui \
  -p 8501:8501 \
  -e BACKEND_URL=http://backend:8000 \
  frontend-ui:latest
```

### Запуск с docker-compose

```bash
# Из корневой директории проекта
docker-compose up -d frontend
```

## Переменные окружения

| Переменная | Описание | Значение по умолчанию |
|------------|----------|----------------------|
| `BACKEND_URL` | URL Backend API Service | `http://backend:8000` |

## Доступ к интерфейсу

После запуска интерфейс доступен по адресу:
```
http://localhost:8501
```

## Использование

### 1. Загрузка диаграммы

1. Откройте веб-интерфейс в браузере
2. Нажмите "Browse files" или перетащите файл в область загрузки
3. Выберите файл диаграммы (PNG, JPG, BPMN, PlantUML, Mermaid, Draw.io)

### 2. Распознавание

1. После загрузки файла появится превью (для изображений)
2. Нажмите кнопку "🚀 Распознать диаграмму"
3. Дождитесь завершения обработки (до 20 секунд)
4. Результат отобразится в виде таблицы с шагами алгоритма

### 3. Просмотр метаданных

После успешного распознавания отображаются:
- Время обработки
- Время инференса
- Используемая модель
- Устройство (CPU/GPU)
- Подробная информация (в раскрывающемся блоке)

### 4. Просмотр истории

1. Перейдите на вкладку "📜 История"
2. Просмотрите список последних запросов
3. Раскройте любой запрос для просмотра деталей
4. Нажмите "🔄 Обновить" для обновления списка

## Скриншоты функционала

### Главная страница
- Заголовок с иконкой
- Информационный блок с инструкциями
- Область загрузки файла
- Превью изображения
- Кнопка распознавания

### Результат распознавания
- Markdown таблица с описанием алгоритма
- Метрики производительности
- Подробная информация в JSON формате

### Sidebar
- Статус backend (зеленый/красный индикатор)
- Список поддерживаемых форматов
- Статистика:
  - Всего запросов
  - Успешных запросов
  - Success Rate
  - Среднее время обработки
  - Статистика по устройствам

## Кастомизация

### Изменение темы

Streamlit поддерживает темы. Создайте файл `.streamlit/config.toml`:

```toml
[theme]
primaryColor = "#1f77b4"
backgroundColor = "#ffffff"
secondaryBackgroundColor = "#f0f2f6"
textColor = "#262730"
font = "sans serif"
```

### Добавление логотипа

Разместите файл `logo.png` в директории `app/` и добавьте в код:

```python
st.sidebar.image("logo.png", use_container_width=True)
```

## Troubleshooting

### Backend недоступен

**Проблема:** Красный индикатор "❌ Backend недоступен"

**Решение:**
1. Проверьте, что backend контейнер запущен: `docker ps | grep backend`
2. Проверьте переменную `BACKEND_URL`
3. Проверьте сетевое соединение между контейнерами
4. Проверьте логи backend: `docker logs backend`

### Ошибка при загрузке файла

**Проблема:** "Ошибка сервера: 400/500"

**Решение:**
1. Проверьте формат файла (должен быть в списке поддерживаемых)
2. Проверьте размер файла (не должен быть слишком большим)
3. Проверьте логи backend для деталей ошибки

### Timeout при обработке

**Проблема:** "⏱️ Превышено время ожидания"

**Решение:**
1. Увеличьте timeout в коде (по умолчанию 120 секунд)
2. Проверьте загрузку VLM сервиса
3. Уменьшите размер входного изображения

### Статистика не отображается

**Проблема:** "Статистика недоступна"

**Решение:**
1. Проверьте, что база данных инициализирована
2. Проверьте эндпоинт `/api/v1/statistics` в backend
3. Проверьте логи backend

## Разработка

### Локальный запуск

```bash
cd app
pip install -r requirements.txt

# Установите переменную окружения
export BACKEND_URL=http://localhost:8000

# Запуск
streamlit run main.py
```

Интерфейс откроется автоматически в браузере на `http://localhost:8501`

### Hot reload

Streamlit автоматически перезагружает приложение при изменении кода. Просто сохраните файл и обновите страницу в браузере.

### Отладка

Добавьте в код для отладки:

```python
st.write("Debug info:", variable)
st.json(data)
```

## Производительность

### Оптимизация

1. **Кэширование** - используйте `@st.cache_data` для кэширования данных
2. **Сессии** - используйте `st.session_state` для хранения состояния
3. **Lazy loading** - загружайте данные только когда нужно

### Мониторинг

Streamlit предоставляет встроенный профайлер:

```bash
streamlit run main.py --server.enableStaticServing=true
```

## Интеграция с другими сервисами

Frontend взаимодействует только с Backend API:

```
Frontend (Streamlit) → Backend API → VLM Service
                                   → Adapter Service
                                   → Database
```

См. корневой `docker-compose.yml` для полной конфигурации.

## Безопасность

### CORS

Backend настроен с разрешающей CORS политикой для разработки. В продакшне ограничьте домены.

### Аутентификация

Для добавления аутентификации используйте:
- Streamlit Authenticator
- OAuth через reverse proxy (nginx)
- Basic Auth на уровне nginx

## Масштабирование

Streamlit не предназначен для highload. Для продакшна рассмотрите:

1. **Nginx** - как reverse proxy с кэшированием
2. **Несколько реплик** - запустите несколько контейнеров frontend
3. **Load balancer** - распределяйте нагрузку между репликами
4. **CDN** - для статических ресурсов

## Альтернативы

Если Streamlit не подходит, рассмотрите:
- **React/Vue.js** - для более сложного UI
- **FastAPI + Jinja2** - для серверного рендеринга
- **Gradio** - альтернатива Streamlit для ML моделей